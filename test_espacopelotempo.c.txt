#include <assert.h>
#include <stdio.h>

#include "function_espacopelotempo.c"

int main() {

    assert(calcularEspaco(80, 2, 0) == 160);

    assert(calcularEspaco(40, 12, 0) == 480);

    assert(calcularEspaco(40, 2, 3) == 86);

    printf("Todos os testes passaram!\n");

    printf("Criado por Ana");

    return 0;
}